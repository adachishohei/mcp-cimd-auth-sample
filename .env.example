# AWS Configuration
# Required for AWS SDK and services
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=123456789012

# ============================================
# Auth Proxy Configuration
# ============================================
# These variables are used by the authorization proxy Lambda functions
# (/authorize and /token endpoints)

# DynamoDB table name for storing PKCE session data
# Required by: authorize.ts, token.ts
SESSION_TABLE_NAME=mcp-auth-sessions

# Cognito User Pool domain prefix (without .auth.region.amazoncognito.com)
# Example: If your domain is "mcp-auth-123456789012.auth.us-east-1.amazoncognito.com"
# Set this to: mcp-auth-123456789012
# Required by: authorize.ts, token.ts
COGNITO_DOMAIN=

# Cognito User Pool Client ID
# Required by: authorize.ts, token.ts
COGNITO_CLIENT_ID=

# AWS region for Cognito (defaults to AWS_REGION if not set)
# Required by: authorize.ts, token.ts
COGNITO_REGION=us-east-1

# ============================================
# MCP Server Configuration
# ============================================
# These variables are used by the MCP server Lambda functions
# (metadata endpoint and MCP protocol handler)

# Base URI of the MCP server (API Gateway URL without trailing slash)
# Example: https://abc123.execute-api.us-east-1.amazonaws.com/prod
# Required by: metadata.ts, mcp-handler.ts
MCP_SERVER_URI=

# Base URI of the auth proxy (API Gateway URL without trailing slash)
# Example: https://xyz789.execute-api.us-east-1.amazonaws.com/prod
# Required by: metadata.ts
AUTH_PROXY_URI=

# Cognito User Pool ID (format: region_id, e.g., us-east-1_ABC123)
# Required by: jwt-middleware.ts, mcp-handler.ts
COGNITO_USER_POOL_ID=

# Comma-separated list of supported OAuth scopes
# Default: mcp:tools
# Optional: metadata.ts
SUPPORTED_SCOPES=mcp:tools

# ============================================
# Deployment Notes
# ============================================
# After deploying the CDK stack, you will need to:
# 1. Copy the Cognito User Pool ID, Client ID, and Domain from CDK outputs
# 2. Copy the API Gateway URLs for Auth Proxy and MCP Server from CDK outputs
# 3. Update this file with the actual values
# 4. Redeploy the Lambda functions with the updated environment variables
